{
  "name": "fyc",
  "version": "1.0.0",
  "main": "backend/index.js",
  "scripts": {
    "build": "cd frontend && npx vite build && rm -rf ../backend/public && mv dist ../backend/public",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "sass": "^1.79.5",
    "cors": "^2.8.5",
    "express": "^4.21.1"
  }
}
